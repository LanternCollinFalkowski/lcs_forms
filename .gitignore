node_modules/
.next/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
backend/prisma/*.db
backend/prisma/*.db-journal

# Resident data never goes in git — this repo is public. Put the tenant list
# CSV here locally; `npm run seed` imports it if present.
data/
*.csv
*.xlsx
*.pdf
# Local Claude Code settings
.claude/settings.local.json
